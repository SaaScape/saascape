server {
        listen 80;
        listen [::]:80;
        server_name <DOMAIN>;

        location /.well-known/acme-challenge {
          root /var/www/<DOMAIN>;
        }

        #<SAASCAPE_APPLICATION_INCLUDE>

       #<SAASCAPE_FILE_SYSTEM_ROOT>
}

